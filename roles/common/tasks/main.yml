---
- name: Ensure ~/.local/bin exists
  ansible.builtin.file:
    path: "{{ local_bin }}"
    state: directory
    mode: "0755"

- name: Ensure ~/.config exists
  ansible.builtin.file:
    path: "{{ config_dir }}"
    state: directory
    mode: "0755"
