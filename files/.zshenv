export ZDOTDIR="$HOME/.config/zsh"
skip_global_compinit=1

# Mise installs its own executable here. Its activation script adds the shims;
# do not add language-specific paths globally.
export PATH="$HOME/.local/bin:$PATH"
