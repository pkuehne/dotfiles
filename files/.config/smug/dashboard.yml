session: dashboard
root: ~
windows:
  - name: dashboard
    layout: tiled
    panes:
      - commands:
          - btop
      - commands:
          - watch -n5 "docker ps -a"
      - commands:
          - watch -n300 "apt list --upgradable 2>/dev/null | tail -n +2"
